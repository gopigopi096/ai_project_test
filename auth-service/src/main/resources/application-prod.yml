spring:
  datasource:
    url: jdbc:postgresql://auth-db:5432/ihms_auth

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

logging:
  level:
    root: INFO

