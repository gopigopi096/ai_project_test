spring:
  datasource:
    url: jdbc:postgresql://appointment-db:5432/ihms_appointment

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

logging:
  level:
    root: INFO

